// 3 - Crie uma função que recebe 2 parâmetros e retorna o resultado da divisão entre eles. Além disso, se o resto 
// dessa divisão for zero deverá imprimir no console o valor e dizer que ele é par.

function divisao (n1,n2) {
    return n1/n2
}
    console.log(divisao(8,8))

    resto = (8 % 8)

    if (resto === 0 ) {
        console.log("O resto é 0, par")
    } 

